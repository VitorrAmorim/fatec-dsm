#Soma de Números

"""
Crie um programa que solicite ao usuário para inserir números e some esses números até que o usuário insira zero. Quando zero for inserido, o programa deve imprimir a soma total.
"""



"""# Entrada
diarias = int(input("Informe o número de diárias: "))

# Cálculo do valor total
valor_diaria = 120
total = diarias * valor_diaria

# Verificação de desconto
if diarias > 7:
    desconto = total * 0.10
    total -= desconto

# Saída
print(f"Valor total da hospedagem: R$ {total:.2f}")
"""

"""
2 - Sistema de Restaurante – Pedido com Adicional
Um sistema de restaurante oferece dois pratos:
• Prato A: R$ 20
• Prato B: R$ 25
O cliente pode ainda adicionar uma bebida por R$ 5.
Desenvolva um algoritmo que pergunte qual prato foi escolhido (A ou B), se
deseja bebida (S ou N) e calcule o valor total do pedido.
"""

"""
total = 0.0

while True:
    valor = float(input("Digite o valor do pedido (ou 0 para finalizar): "))
    
    if valor == 0:
        break
    
    total += valor

print(f"Total da conta: R$ {total:.2f}")
"""

"""
contador = 0

while contador < 3:
    input("Digite o nome da pessoa: ")  # Apenas para simular o registro
    contador += 1
    print("Pessoa registrada.")

print("Limite atingido.")

"""